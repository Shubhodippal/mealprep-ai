name = "mealprep-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# The Anthropic API key is NOT stored here. Set it as an encrypted secret:
#   cd proxy
#   npx wrangler secret put ANTHROPIC_API_KEY
